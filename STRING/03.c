#include<stdio.h>
#include<string.h>
main()
{
	//3. WAP to find the length of a given string.
	
	char name[20];
	printf("Enter value n:");
	gets(name);
	int i,x;
	
	 x=strlen(name);
	 printf("%d ",x);
}
